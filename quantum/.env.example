# Copy this file to ".env.local" (which is git-ignored) and paste your key.
# Get the key from your account at https://account.originqc.com.cn/
# This file is the ONLY place the key should ever live. Never commit the real key,
# and never put it in any website's frontend code — static sites expose secrets to everyone.

QPANDA3_API_KEY=paste-your-origin-quantum-api-key-here
